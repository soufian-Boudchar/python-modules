#arctic_frost
#berry_smoothie
#blueprint
#bumblebee
#dracula
#grayscale_pro
#sunset_blvd
#vaporwave


THEMES = {
    "vaporwave": {
        "walls":  "\033[38;5;55m██\033[0m",    # Deep Purple
        "path":   "\033[38;5;218m██\033[0m",   # Pastel Pink
        "enter":  "\033[38;5;51m██\033[0m",    # Cyan
        "exit":   "\033[38;5;201m██\033[0m",   # Hot Magenta
        "logo":   "\033[38;5;49m██\033[0m"     # Spring Green
    },
    "dracula": {
        "walls":  "\033[38;5;236m██\033[0m",   # Dark Charcoal
        "path":   "\033[38;5;60m██\033[0m",    # Muted Blue-Grey
        "enter":  "\033[38;5;212m██\033[0m",   # Soft Pink
        "exit":   "\033[38;5;141m██\033[0m",   # Lavender
        "logo":   "\033[38;5;84m██\033[0m"     # Pale Green
    },
    "Shadow_Protocol": {
        "walls":  "\033[48;5;235m  \033[0m",
        "path":   "\033[48;5;250m  \033[0m",
        "enter":  "\033[48;5;118m  \033[0m",
        "exit":   "\033[48;5;202m  \033[0m",
        "logo":   "\033[48;5;220m  \033[0m"
    },
    "Nordic Frost": {
        "walls":  "\033[48;5;60m  \033[0m",
        "path":   "\033[48;5;153m  \033[0m",
        "enter":  "\033[48;5;150m  \033[0m",
        "exit":   "\033[48;5;203m  \033[0m",
        "logo":   "\033[48;5;37m  \033[0m" 
    },
    "sunset_blvd": {
        "walls":  "\033[38;5;54m██\033[0m",    # Indigo
        "path":   "\033[38;5;203m██\033[0m",   # Salmon Pink
        "enter":  "\033[38;5;226m██\033[0m",   # Sun Yellow
        "exit":   "\033[38;5;166m██\033[0m",   # Deep Orange
        "logo":   "\033[38;5;213m██\033[0m"    # Orchid
    },
    "grayscale_pro": {
        "walls":  "\033[48;5;237m  \033[0m",   # Near Black background
        "path":   "\033[48;5;250m  \033[0m",   # Light Gray background
        "enter":  "\033[48;5;255m  \033[0m",   # White background
        "exit":   "\033[48;5;242m  \033[0m",   # Medium Gray background
        "logo":   "\033[48;5;223m  \033[0m"    # Dark Gray background
    },

    "bumblebee": {
        "walls":  "\033[48;5;237m  \033[0m",   # Dark Gray background, 2 spaces for full block
        "path":   "\033[48;5;227m  \033[0m",   # Pale Yellow
        "enter":  "\033[48;5;255m  \033[0m",   # White
        "exit":   "\033[48;5;214m  \033[0m",   # Orange-Gold
        "logo":   "\033[48;5;226m  \033[0m"    # Pure Yellow
    },
    "berry_smoothie": {
        "walls":  "\033[38;5;53m██\033[0m",    # Deep Purple/Maroon
        "path":   "\033[38;5;161m██\033[0m",   # Raspberry
        "enter":  "\033[38;5;183m██\033[0m",   # Light Lilac
        "exit":   "\033[38;5;89m██\033[0m",    # Grape
        "logo":   "\033[38;5;197m██\033[0m"    # Bright Pink
    },
    "neon_nights": {  # High contrast Cyberpunk
        "walls":  "\033[38;5;234m██\033[0m",    # Pitch Black
        "path":   "\033[38;5;39m██\033[0m",     # Electric Blue
        "enter":  "\033[38;5;82m██\033[0m",     # Neon Green
        "exit":   "\033[38;5;198m██\033[0m",    # Hot Pink
        "logo":   "\033[38;5;226m██\033[0m"     # Warning Yellow
    },
    "ocean_breeze": { # Clean and calming
        "walls":  "\033[38;5;23m██\033[0m",     # Deep Sea Teal
        "path":   "\033[38;5;45m██\033[0m",     # Light Blue
        "enter":  "\033[38;5;123m██\033[0m",    # Aqua
        "exit":   "\033[38;5;208m██\033[0m",    # Coral Orange
        "logo":   "\033[38;5;51m██\033[0m"      # Cyan
    },
    "cherry_blossom": { # Modern Aesthetic
        "walls":  "\033[38;5;52m██\033[0m",     # Dark Brown/Red
        "path":   "\033[38;5;218m██\033[0m",    # Pastel Pink
        "enter":  "\033[38;5;255m██\033[0m",    # White
        "exit":   "\033[38;5;161m██\033[0m",    # Deep Red
        "logo":   "\033[38;5;212m██\033[0m"     # Soft Pink
    },
    "desert_dune": { # Warm and minimal
        "walls":  "\033[38;5;94m██\033[0m",     # Earth Brown
        "path":   "\033[38;5;221m██\033[0m",    # Sand Gold
        "enter":  "\033[38;5;229m██\033[0m",    # Light Cream
        "exit":   "\033[38;5;208m██\033[0m",    # Sunset Orange
        "logo":   "\033[38;5;214m██\033[0m"     # Goldenrod
    },
    "arctic_aurora": { # Cool dark mode
        "walls":  "\033[38;5;234m██\033[0m",    # Dark Grey
        "path":   "\033[38;5;43m██\033[0m",     # Cyan-Turquoise
        "enter":  "\033[38;5;39m██\033[0m",     # Deep Sky Blue
        "exit":   "\033[38;5;129m██\033[0m",    # Purple
        "logo":   "\033[38;5;49m██\033[0m"      # Spring Green
    },
    "golden_hour": { # Luxury/Sunset
        "walls":  "\033[38;5;54m██\033[0m",     # Deep Indigo
        "path":   "\033[38;5;214m██\033[0m",    # Orange Gold
        "enter":  "\033[38;5;227m██\033[0m",    # Pale Yellow
        "exit":   "\033[38;5;166m██\033[0m",    # Burnt Orange
        "logo":   "\033[38;5;172m██\033[0m"     # Bronze
    }
    
}



import random
import os
import time
import sys

WIDTH = 50
HEIGHT = 50

ENTER = (0, 0)
EXIT = (49,  49)

TOP = 1
BOTTOM = 4
RIGHT = 2
LEFT = 8

SEED = 1337
random.seed(SEED)


class Cell:

    def __init__(self, x, y):
        self.x = x
        self.y = y
        self.walls = 15  # TOP: 0001 / RIGHT: 0010 / BOTTOM: 0100 / LEFT: 1000
        self.traffic = ""
        self.visited = False


class Maze:

    def __init__(self, width, height):
        self.width = width
        self.height = height
        self.grid = [[Cell(x, y) for x in range(width)] for y in range(height)]
        self.stack = []

    def get_cell(self, x, y):
        if 0 <= x < self.width and 0 <= y < self.height:
            return self.grid[y][x]
        return None

    def check_neighbors(self, cell):
        neighbors = []

        directions = [
            (0, -1),  #Top
            (0, 1),  #Bottom
            (1, 0),  #Right
            (-1, 0)  #Left
        ]

        for dx, dy in directions:
            neighbor = self.get_cell(cell.x + dx, cell.y + dy)

            if neighbor and not neighbor.visited:
                neighbors.append(neighbor)
        return neighbors

    def remove_wall(self, current, next_cell):
        dx = next_cell.x - current.x
        dy = next_cell.y - current.y

        if dx == 1:
            current.walls -= RIGHT
            next_cell.walls -= LEFT
        elif dx == -1:
            current.walls -= LEFT
            next_cell.walls -= RIGHT
        elif dy == 1:
            current.walls -= BOTTOM
            next_cell.walls -= TOP
        elif dy == -1:
            current.walls -= TOP
            next_cell.walls -= BOTTOM

    def generate_maze(self):
        start_cell = self.grid[0][0]
        start_cell.visited = True
        self.stack.append(start_cell)

        while len(self.stack) > 0:
            current = self.stack[-1]

            neighbors = self.check_neighbors(current)

            if neighbors:
                next_cell = random.choice(neighbors)
                self.remove_wall(current, next_cell)
                next_cell.visited = True
                self.stack.append(next_cell)
            else:
                self.stack.pop()


def delete_visited(maze):
    for row in maze.grid:
        for cell in row:
            cell.visited = False


def mark_visited(cell):
    cell.visited = True
    

def solve_maze(maze, ENTERY, EXIT):
    start_cell = ENTERY
    end_cell = EXIT
    delete_visited(maze)

    mark_visited(start_cell)
    stack = [start_cell]

    while len(stack) > 0:
        current = stack[-1]

        if current == end_cell:
            return stack

        neighbors = []
        x = current.x
        y = current.y

        if y > 0 and not (current.walls & 1):  # --> check up
            neighbors.append(maze.grid[y - 1][x])

        if y < maze.height - 1 and not (current.walls & 4):  # --> check down
            neighbors.append(maze.grid[y + 1][x])

        if x < maze.width - 1 and not (current.walls & 2):  # --> check right
            neighbors.append(maze.grid[y][x + 1])

        if x > 0 and not (current.walls & 8):  # --> check left
            neighbors.append(maze.grid[y][x - 1])

        valid_neighbor = None
        for n in neighbors:
            if not n.visited:
                valid_neighbor = n
                break

        if valid_neighbor:
            mark_visited(valid_neighbor)
            stack.append(valid_neighbor)

            dx = valid_neighbor.x - current.x
            dy = valid_neighbor.y - current.y

            if dy == -1:  # -> check up / North
                valid_neighbor.traffic = "N"
            elif dy == 1:  # check down / South
                valid_neighbor.traffic = "S"
            elif dx == 1:  # check right / East
                valid_neighbor.traffic = "E"
            elif dx == -1:  # check left / West
                valid_neighbor.traffic = "W"

        else:
            stack.pop()

    return []


def draw_42(maze_grid):
    x_index_42 = (WIDTH // 2) - 3
    y_index_42 = (HEIGHT // 2) - 2
    grid_42 = [[0, 4, 5, 6], [0, 6], [0, 1, 2, 4, 5, 6], [2, 4], [2, 4, 5, 6]]

    for row_index, row in enumerate(grid_42):
        for i in row:
            maze_grid[y_index_42 + row_index][x_index_42 + i].visited = True

    


def print_maze(maze, path, theme):

    print(f"{theme["walls"]}" * (maze.width * 2 + 1))

    for y in range(len(maze.grid)):
        maze_way = f"{theme["walls"]}"
        bottom = f"{theme["walls"]}"

        for x in range(len(maze.grid[y])):
            cell = maze.grid[y][x]
            
            #Center logic
            if x == EXIT[0] and y == EXIT[1]:
                maze_way += f"{theme["exit"]}"
            elif x == ENTER[0] and y == ENTER[1]:
                maze_way += f"{theme["enter"]}"
            elif cell in path:
                maze_way += f"{theme["path"]}"
            elif cell.walls == 15:
                maze_way += f"{theme["logo"]}"
            else:
                maze_way += "  "
                
                
            #Right logic
            if cell.walls & 2:
                maze_way += f"{theme["walls"]}"
            else:
                if x + 1 < maze.width:
                    right = maze.grid[y][x + 1]
                    if cell in path and right in path:
                        maze_way += f"{theme["path"]}"
                    else:
                        maze_way += "  "
                else:
                    maze_way += "  "
            #Bottom logic
            if cell.walls & 4:
                bottom += f"{theme["walls"]}"
            else:
                if y + 1 < maze.height:
                    down = maze.grid[y + 1][x]
                    if cell in path and down in path:
                        bottom += f"{theme["path"]}"
                    else:
                        bottom += "  "
                else:
                    bottom += "  "

            bottom += f"{theme["walls"]}"

        print(maze_way)
        print(bottom)


def output_maze(maze, path):
    output = open("output_maze.txt", "w")

    for row in maze.grid:
        for cell in row:
            output.write(format(cell.walls, "X"))
        output.write("\n")
    output.write("\n")
    output.write(f"{ENTER[0]},{ENTER[1]}\n")
    output.write(f"{EXIT[0]},{EXIT[1]}\n")
    for cell in path:
        output.write(cell.traffic)
    output.close()

def maze_animating(maze, path):
    
    theme_name = random.choice(list(THEMES.keys()))
    theme = THEMES[theme_name]
    tpath = []
    print("\033[2J")

    for _ in path[:]:
        if path:
            tpath.append(path.pop(0))

        print("\033[H", end="")
        print_maze(maze, tpath, theme)

        sys.stdout.flush()
        time.sleep(0.01)

    print("\033[H", end="")
    print_maze(maze, tpath, theme)



maze = Maze(WIDTH, HEIGHT)
draw_42(maze.grid)
maze.generate_maze()
path = solve_maze(maze, maze.grid[ENTER[1]][ENTER[0]], maze.grid[EXIT[1]][EXIT[0]])
output_maze(maze, path)
maze_animating(maze, path)
